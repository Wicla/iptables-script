This project contains various IPTables-scripts.
Currently they're seperated in two different directories:
 * client
 * server

There're at least two branches, main and locallyModified.
locallyModified contains a version where ports are opened.
